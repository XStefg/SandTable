<Project>
  <PropertyGroup>

    <OutputPath>$(SolutionDir)$(Configuration)</OutputPath>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <Nullable>enable</Nullable>

    <CoreRepositoryDir>$(MSBuildThisFileDirectory)..\Core\</CoreRepositoryDir>
  </PropertyGroup>
</Project>